# QuizRepo

Start app on port 8080 (or edit baseUrl in the environment).

In Postman: Import → Upload Files → pick both JSON files.

Select the “Quiz App Local” environment.

Run requests top-to-bottom:

Create Quiz (returns 201; captures IDs)

Get Quiz

Submit Quiz
